name: fwf
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
          echo "hello world"
          echo "world, hello"
      - name: Print Goodbye
        run: echo "done bye"
